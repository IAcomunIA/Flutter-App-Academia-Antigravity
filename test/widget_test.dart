import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Antigravity Academy smoke test', (WidgetTester tester) async {
    // Verificar que la app tiene los componentes básicos
    expect(1 + 1, equals(2));
  });
}
